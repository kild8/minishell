//le but est de transformer les tokens en commandes
//et de les free du coup
void	transformer(t_token **tok)
{
	t_token	*current;

	current = *tok;
	while (current)
	{




	}
	free_token(tok);
	return ;
}