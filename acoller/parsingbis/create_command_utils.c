int	count_arg(char *input)
{
	int	n;
	int	i;

	while (str[i])
	{



		i++
	}
	return (n);
}

int	arg_size()

c la que jai eu lidee de faire un static int qui sera le curseur de ou on en est dans la ligne dinput
pour reprendre depuis la
donc on y va jusqua un espace
attention on a rajouter aussi le fait que les > et < ne sont pas des arguments
donc le static peut sarreter ausssi aux <> et appeler la fonction crete inpath ou outpath
puis il reprend et continu sa lecture
oui cest le mieux !
donc cest dans le parsing input

int	comman